function pinCode(input) {
    let furstNum = Number(input[0]);
    let secondNum = Number(input[1]);
    let thurdNum = Number(input[2]);

    let firstNum = 0;
    let secNum = 0;
    let thrdNum = 0;


    for (let i = 1; i <= furstNum; i++) {
        if (i % 2 === 0) {
            firstNum = i;
        }
        for (let j = secondNum; j < 7; j++) {

        }

    }

    console.log(firstNum)

}
pinCode([3, 5, 5])